<h1>"<%=@task.id%>番目のタスクを編集"</h1>

<%= form_for(@task) do |f|%>
  <%= f.label :content, "新規タスク"%>
  <%= f.text_field :content %>
  
  <%= f.submit "登録"%>
<% end %>

<%= link_to "タスクの一覧へ戻る", tasks_path %>